#include "../include/EventHandler.h"

EventHandler::EventHandler(void* m_sender, void* m_parent, const float& m_clock):
                sender(m_sender), 
                parent(m_parent), 
                clock(m_clock) 
{
    
};